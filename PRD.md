# 모발이식 병원 전후사진 표준화 도구 PRD

## 0. 문서 정보

| 항목 | 내용 |
|---|---|
| 문서 버전 | v0.1 (초안) |
| 작성일 | 2026-09-01 |
| 작성자 | (병원명 입력) |
| 상태 | 초안 — 병원 내부 검토 대기 |

---

## 1. 개요

### 1.1 배경
모발이식 병원에서 전후(before/after) 사진은 상담·마케팅·시술 기록의 핵심 자료다. 그러나 촬영 시점마다 카메라 거리, 얼굴/두상 각도, 조명, 화이트밸런스가 달라 같은 환자의 사진이라도 비교하기 어렵고, 실제보다 과장되거나 왜곡된 비교로 보일 위험이 있다. 또한 마케팅·SNS·블로그에 활용할 때 환자 식별 정보(얼굴)를 가려야 하는데, 모발이식의 경우 시술 결과를 보여주는 핵심 부위가 **헤어라인과 눈썹**이라 얼굴 전체를 가리면서도 이 두 부위는 반드시 노출되어야 한다. 현재는 포토샵 등으로 이 작업을 수작업으로 하고 있어 시간이 오래 걸리고 담당자마다 결과물 품질이 다르다.

### 1.2 목적
전/후 사진 한 쌍을 업로드하면 자동으로 **① 동일한 크기(스케일) ② 동일한 각도 ③ 동일한 밝기 ④ 동일한 색감**으로 정렬·보정하고, 필요 시 **얼굴 모자이크**까지 적용해 결과물을 즉시 내려받을 수 있는 웹 애플리케이션을 만든다.

### 1.3 사용 주체
- 단일 병원(1개 지점) 내부 도구
- 원장, 마케팅팀, 실장이 함께 사용하며 **역할 구분 없이 동일한 권한**으로 접근 (전 계정이 원본 열람·업로드·모자이크·다운로드 모두 가능)

> ⚠️ 오픈 이슈: 전체 동일 권한으로 결정했으나, 마케팅팀 계정으로도 미보정 원본(모자이크 전) 이미지에 접근 가능해진다는 의미이기도 하다. 이는 곧 병원 내부 개인정보 취급 방침과 연결되는 부분이라 8장에서 별도로 다시 짚는다. MVP 이후 "원본 열람은 원장/실장만, 마케팅팀은 모자이크 결과물만" 형태로 세분화할 여지를 열어두는 것을 권장.

---

## 2. 문제 정의

| 현재 문제 | 원인 | 이 제품으로 해결하는 방식 |
|---|---|---|
| 전/후 사진 비교 시 크기·각도가 달라 왜곡되어 보임 | 촬영 거리·환자 자세가 매번 다름 | 얼굴 랜드마크 기준 자동 정렬·스케일 정규화 |
| 조명·화이트밸런스 차이로 피부톤이 달라 보임 | 촬영 환경(조도, 카메라 세팅)이 매번 다름 | 밝기/색감 자동 매칭(보정) |
| SNS·블로그 게시 전 얼굴 비식별화가 수작업 | 담당자가 포토샵으로 매번 개별 처리 | 얼굴 모자이크 자동화 |
| 결과물 품질이 담당자마다 다름 | 표준화된 도구 부재 | 동일 알고리즘으로 일관된 결과 보장 |

---

## 3. 목표 및 성공 지표 (MVP 기준)

| 목표 | 지표 |
|---|---|
| 보정 작업 시간 단축 | 사진 1쌍당 수작업 대비 처리 시간 80% 이상 단축 (예: 5분 → 1분 이내) |
| 결과물 품질 표준화 | 담당자 관계없이 동일 입력 → 동일 출력 (알고리즘 재현성) |
| 실사용 정착 | 배포 4주 내 원장/실장/마케팅팀 전원이 주 1회 이상 사용 |
| 모자이크 신뢰도 | 얼굴 미검출/오검출로 인한 재작업 비율 5% 미만 |

---

## 4. 사용자 시나리오

1. **원장/실장**: 상담 중 환자에게 유사 케이스의 전후사진을 비교해서 보여줄 때, 왜곡 없이 정렬된 사진을 즉시 조회한다.
2. **마케팅팀**: 블로그·인스타그램에 올릴 전후사진을 선택하고, 얼굴을 모자이크 처리한 결과물을 다운로드한다.
3. **실장**: 환자 시술 전/후 사진을 촬영 직후 업로드하고, 시스템이 자동 정렬·보정한 결과를 환자 차트(또는 별도 폴더)에 저장한다.

---

## 5. 범위

### 5.1 In Scope (MVP)
- 전/후 사진 업로드 (환자별 페어링)
- 얼굴 랜드마크 기반 자동 정렬 (각도 보정 + 크기 정규화)
- 밝기/색감 자동 보정 (전 사진 또는 후 사진 중 하나를 기준으로 매칭)
- 얼굴 모자이크(비식별화) 적용/해제 토글
- 결과 미리보기 (전/후 나란히 비교 뷰)
- 결과 이미지 다운로드 (개별 / 전후 합성 1장)
- 환자(또는 케이스)별 사진 목록 관리, 검색

### 5.2 Out of Scope (MVP 제외, 추후 검토)
- 모바일 앱
- 여러 지점(분원) 지원, 지점 간 데이터 분리
- 역할별 세분화된 권한 관리 (6장 오픈 이슈 참고)
- 전자차트(EMR) 연동
- 다중 사진(3장 이상) 시퀀스 비교, 동영상
- 시술 결과 자동 분석/추천 등 AI 진단 기능

---

## 6. 핵심 기능 요구사항

### 6.1 사진 업로드 및 페어링
- 전/후 사진을 각각 업로드하거나, 한 번에 2장을 드래그 앤 드롭으로 업로드
- 지원 포맷: JPG, PNG, HEIC(아이폰 촬영 대응)
- 환자명 또는 환자 ID, 시술 방식(모발이식 방식·이식 부위 등 태그), 촬영 각도(정면/측면/정수리 등), 촬영일자를 메타데이터로 입력
- 업로드 시 얼굴이 검출되지 않으면 경고 및 수동 재시도 안내

### 6.2 얼굴 랜드마크 검출 및 자동 정렬 (각도 보정)
- 얼굴 랜드마크(눈, 코, 입 등) 검출 라이브러리를 사용해 기준점 추출
- 두 눈의 중심을 잇는 선이 수평이 되도록 회전 보정 (roll 보정)
- 정면 대비 좌우 기울어짐(yaw)이 심한 사진은 "정렬 신뢰도 낮음" 경고 표시 (완전 자동 3D 보정은 MVP 범위 밖)
- 기준 사진(보통 "전" 사진)에 "후" 사진을 맞추는 방식을 기본값으로 하되, 사용자가 기준을 바꿀 수 있음

### 6.3 크기(스케일) 정규화
- 얼굴 랜드마크 간 거리(예: 양 눈동자 간 거리, 또는 얼굴 폭)를 기준 척도로 사용해 두 사진의 스케일을 통일
- 정렬 후 공통 영역으로 자동 크롭하거나, 캔버스 크기를 통일하고 빈 여백은 지정 색상(흰색/검정) 채움 — 두 방식 중 선택 가능한 옵션 제공

### 6.4 밝기/색감 자동 보정
- 밝기(휘도)와 색감(화이트밸런스/색조)을 두 사진 간에 매칭
- 방식: 히스토그램 매칭 또는 색 전이(color transfer) 알고리즘 기반, 기준 사진의 밝기·색감에 다른 사진을 맞춤
- 보정 강도 조절 슬라이더(0~100%) 제공 — 과보정으로 인한 피부톤 왜곡 우려에 대응
- 원본 대비 Before/After 토글로 보정 전후 비교 가능

### 6.5 얼굴 모자이크(비식별화) — 헤어라인·눈썹 노출 방식
모발이식 병원 특성상 시술 결과(헤어라인, 이마 라인, 눈썹)는 반드시 보여야 하고, 그 외 얼굴 전체(눈, 코, 입, 볼, 턱 등 식별 가능한 부위)는 가려야 한다.

- 얼굴 랜드마크 검출로 **헤어라인 영역(이마 상단~두상 경계)** 과 **눈썹 영역**의 좌표를 특정
- 해당 두 영역만 남기고 **나머지 얼굴 전체를 모자이크(픽셀화) 또는 블러 처리**
  - 즉 기존의 "얼굴 전체 가리기" 방식에서 헤어라인·눈썹 부분만 마스크로 제외(cut-out)하는 방식
- 헤어라인/눈썹 노출 영역의 여유값(margin)을 조절 가능하게 하여, 너무 타이트하게 잘려 시술 라인이 잘 안 보이거나 반대로 눈까지 노출되는 문제를 방지
- 모자이크 강도(픽셀 크기) 조절 가능
- 얼굴/헤어라인 자동 검출이 부정확할 경우를 대비해 **수동으로 노출 영역(헤어라인·눈썹)을 드래그로 재조정**할 수 있는 폴백 제공

**촬영 각도별 처리 규칙**

| 촬영 각도 | 모자이크 적용 | 비고 |
|---|---|---|
| 정면 | 얼굴 전체 모자이크, 헤어라인·눈썹만 노출 | 기본 케이스 |
| 측면 | 얼굴 전체 모자이크, 헤어라인·눈썹만 노출 | 정면과 동일 규칙. 눈썹이 측면 각도에서도 보이는 한 동일 로직 적용 |
| 정수리(탑뷰) | **모자이크 미적용** | 얼굴이 프레임에 들어오지 않아 식별 위험이 없으므로 원본 그대로 사용. 단, 얼굴이 함께 촬영된 정수리 사진(고개를 숙인 각도 등)은 자동으로 얼굴 포함 여부를 판별해 정면/측면 규칙으로 전환 |

- 촬영 각도는 6.1의 메타데이터 태그(정면/측면/정수리)를 기준으로 자동 분기하되, 사용자가 수동으로 각도 태그를 수정하면 그에 맞춰 모자이크 규칙도 즉시 재적용

### 6.6 결과 미리보기 및 내보내기
- 전/후 사진을 좌우 또는 위아래로 나란히 배치한 합성 미리보기
- 원본 / 정렬·보정본 / 모자이크 적용본을 각각 개별 다운로드하거나, 합성 1장(워터마크·로고 삽입 옵션 포함)으로 다운로드
- 다운로드 포맷: JPG(기본), PNG(투명 배경 필요시)

### 6.7 사진(케이스) 관리
- 환자/케이스별 사진 목록 조회, 검색(환자명, 시술 부위, 날짜)
- 개별 사진 삭제, 케이스 전체 삭제
- 삭제 시 복구 불가 경고 (또는 30일 휴지통 정책 — 8장 참고)

---

## 7. 사용자 흐름 (User Flow)

```
로그인
  └─ 케이스 목록 (환자별)
       ├─ 신규 케이스 생성
       │    └─ 전 사진 업로드 + 후 사진 업로드
       │         └─ 자동 정렬·밝기·색감 보정 처리 (진행 표시)
       │              └─ 결과 미리보기 (보정 강도 조절 가능)
       │                   ├─ 모자이크 적용 여부/범위 선택
       │                   └─ 다운로드 / 케이스에 저장
       └─ 기존 케이스 열람
            └─ 저장된 결과 재열람 / 재다운로드 / 재보정
```

---

## 8. 비기능 요구사항

### 8.1 성능
- 사진 1쌍 업로드 → 정렬·보정 결과 미리보기까지 5초 이내 (서버 처리 기준)
- 동시 사용자 10명 내외 (내부 도구 특성상 대규모 트래픽 불필요)

### 8.2 정확도
- 정면에 가까운 얼굴 사진 기준 랜드마크 검출 성공률 95% 이상
- 검출 실패/저신뢰 시 사용자에게 명확히 안내하고 수동 보정 옵션 제공

### 8.3 개인정보 및 보안 (중요)
얼굴 사진은 「개인정보 보호법」상 민감정보(생체인식정보)에 해당할 수 있고, 시술 정보와 결합 시 의료정보로서 더 엄격한 보호가 필요하다.

- **전송 구간 암호화**: HTTPS 필수
- **저장 암호화**: 원본 이미지 저장소 암호화(at-rest)
- **접근 로그**: 누가 언제 어떤 환자의 사진을 열람/다운로드했는지 로그 기록 (동일 권한 구조이기 때문에 오남용 방지를 위해 로그가 특히 중요)
- **인증**: 병원 내부 계정 시스템과 연동한 로그인 (아이디/비밀번호 + 필요시 2단계 인증)
- **보관 기간 및 파기 정책**: 환자 동의 철회 또는 퇴원/이탈 시 삭제 절차 정의 필요 (병원 내부 규정과 협의)
- **외부 유출 방지**: 마케팅용으로 다운로드된 모자이크 결과물과, 원본 파일이 섞이지 않도록 다운로드 폴더/파일명 규칙 구분 (예: `원본_비공개` vs `모자이크_배포용` 접두어)
- **환자 동의**: SNS/블로그 활용 목적의 사진은 별도 동의서 확보 여부를 시스템에서 체크(태그)하도록 하여, 동의 없는 사진이 실수로 게시용으로 다운로드되는 것을 방지하는 것을 권장

### 8.4 브라우저/환경 지원
- 최신 Chrome, Edge, Safari 데스크톱 기준 (병원 PC 환경 고려)
- 반응형은 필수 아님 (내부 데스크톱 사용 중심), 단 실장이 태블릿에서 촬영 직후 업로드할 가능성 있다면 태블릿 웹뷰 확인 필요 (오픈 이슈)

---

## 9. 기술 스택 제안 (초안)

| 영역 | 제안 | 비고 |
|---|---|---|
| 프론트엔드 | React/Next.js | 이미지 미리보기, 슬라이더 UI 등에 적합 |
| 백엔드 | Python (FastAPI) | 이미지 처리 라이브러리 생태계가 풍부 |
| 얼굴 랜드마크 검출 | MediaPipe Face Mesh 또는 dlib 68-point | 오픈소스, 온프레미스 처리 가능(외부 API로 얼굴 사진을 보내지 않아도 됨 — 개인정보 측면에서 유리) |
| 이미지 정렬/변환 | OpenCV (affine transform, warp) | |
| 색감/밝기 보정 | 히스토그램 매칭 또는 Reinhard color transfer (OpenCV/NumPy) | |
| 모자이크 처리 | OpenCV (얼굴 영역 검출 후 pixelate/blur) | |
| 저장소 | 병원 내부 서버 또는 사설 클라우드(암호화) | 외부 퍼블릭 클라우드 사용 시 의료정보 취급 관련 병원 내부 규정 확인 필요 |
| 인증 | 자체 계정 시스템 (병원 내부용, 외부 노출 없음) | |

> 얼굴 인식/랜드마크 검출을 외부 API(클라우드 AI 서비스)로 처리할지, 온프레미스(자체 서버)로 처리할지는 민감정보 처리 방침상 중요한 결정이다. 내부 도구이고 의료 사진이라는 점을 고려하면 **온프레미스 처리를 기본 방향으로 권장**한다.

---

## 10. 마일스톤 (제안)

| 단계 | 범위 | 목표 기간 |
|---|---|---|
| M1: 프로토타입 | 업로드 + 랜드마크 검출 + 각도/크기 정렬만 (밝기·모자이크 제외) | 2주 |
| M2: 보정 기능 추가 | 밝기/색감 자동 보정, 강도 조절 | +2주 |
| M3: 모자이크 기능 | 얼굴 모자이크(눈/전체 선택), 수동 보정 폴백 | +1.5주 |
| M4: 케이스 관리 + 배포 | 로그인, 케이스별 관리, 접근 로그, 내부 배포 | +2주 |
| M5: 내부 시범 운영 및 피드백 반영 | 원장/실장/마케팅팀 실사용 피드백 수집 | +2주 |

(기간은 개발 리소스 규모에 따라 조정 필요 — 현재 PRD에는 개발 인력 규모가 반영되어 있지 않음)

---

## 11. 오픈 이슈 / 결정 필요 사항

1. **권한 구조**: 지금은 전체 동일 권한으로 정했지만, 원본(비모자이크) 이미지에 마케팅팀도 접근 가능하다는 의미다. 이대로 진행할지, 추후에라도 "원본 열람 제한" 옵션을 넣을지 병원 내부 방침 확정 필요.
2. ~~정면 외 촬영 각도 대응~~ → **결정 완료** (6.5절 참고): 정면·측면은 헤어라인·눈썹만 노출하고 나머지 모자이크, 정수리는 모자이크 미적용. 단 "얼굴이 함께 찍힌 정수리 사진"의 자동 판별 정확도는 프로토타입 단계에서 검증 필요.
3. **환자 동의 관리**: 시스템 내에서 SNS 활용 동의 여부를 태그로 관리할지, 아니면 별도 종이/전자 동의서 프로세스로 관리하고 이 시스템은 관여하지 않을지.
4. **데이터 저장 위치**: 병원 자체 서버 vs 클라우드. 의료정보 특성상 병원 내부 IT 정책 확인 필요.
5. **기준 사진**: 정렬/보정의 기준을 항상 "전" 사진으로 할지, 사용자가 매번 선택하게 할지.
6. **삭제 정책**: 즉시 영구 삭제 vs 휴지통(예: 30일) 방식.

---

## 12. 용어 정리

- **랜드마크(landmark)**: 눈, 코, 입 등 얼굴의 특징점. 정렬·스케일 계산의 기준이 됨
- **히스토그램 매칭**: 한 이미지의 밝기/색상 분포를 다른 이미지의 분포에 맞추는 보정 기법
- **모자이크(비식별화)**: 얼굴을 픽셀화하거나 흐리게 처리해 인물을 알아볼 수 없게 만드는 처리
